# =============================================================================
# CONFIGURACIÓN DEL ENTORNO — FinalDFS (raíz del proyecto)
# =============================================================================
#   cp .env.example .env
#
# Usado por docker-compose para construir URLs públicas (allocate, heartbeat).
# Credenciales MongoDB y JWT van en namenode/.env (ver namenode/.env.example).
# =============================================================================

# IP pública del host — NameNode :8000, DataNodes :8001-:8003
PUBLIC_IP=52.23.74.126

# Opcional (NameNode — también se pueden definir en docker-compose.yml)
# HEARTBEAT_TIMEOUT_SEC=90
# MONITOR_INTERVAL_SEC=30
